avancement avec recalcul du float si float negatif 


Gérer par organisation users = admin, chef de projet, opc 
admib -> créer un projet modif etc
chef de projet -> créer projet
opc -> accès lecture 

nom du projet -> client id de projet

Dashboard
project id en titre name aussi
start end en haut a gauche
avancement global projet

dashboard partageable client avec visuel smartsheet like
création de vue personnalisé
format en titre et en section sur le gantt 
police segoe ui

tache et dependance onglet unitule a ajouter dans le gantt
sur le gantt besoin d'une tache de date ddebut date fin fin delai ressources zone étage predecesseur successeur marge total (cumul depuis la dependance) = calcul marge libre (loé a la tache) taceh critique y/n

column masquable limite de defilement zoom plus précis 
organisation tab voir project onglet edition affichege projet

ajhout couleur ressources par type (clok ouvert couleur avec degradé de rouge, corps d'état technique dégradé de bleu, corps d'état architecturaux degradé de vert, espaces exterieurs dégradé de jaune maitrise d'ovre/oubre degrade de gris) avoir si affichage couleur ressource ou affichage projet sur gantt soit chemin critique mais pousser le degradé de bleu vert suivant avancement et rouge si retard

ajout indexation 
coluonne plus condensé

=> audit
1. Tasks
   Déjà beaucoup mieux, à vérifier rapidement.

2. Projects
   On vient de le nettoyer, à vérifier rapidement.

3. TaskDependencies
   Très important car il y a l’anti-cycle, les offsets et le recalcul planning.

4. Resources
   CRUD ressources.

5. ResourceGroups
   Groupes + membres + suppression protégée.

6. ResourceAssignments
   Assignation ressource OU groupe.

7. Gantt / Summary / Warnings / ResourceAnalysis
   Endpoints lecture/calcul, à rendre propres mais pas forcément CRUD.


   08/07/2026
   Afficher les dates sur le coté redsuire hauteur

FlutterError (Looking up a deactivated widget's ancestor is unsafe.
At this point the state of the widget's element tree is no longer stable.
To safely refer to a widget's ancestor in its dispose() method, save a reference to the ancestor by calling dependOnInheritedWidgetOfExactType() in the widget's didChangeDependencies() method.)

ajouter niveau de hierarchie

modification plus complete 

calendrier ne change pas les dates lors de changement
ajouter periode non ouvré pour congés

comparaison baselines probleme d'affichage + ajouter graph + gantt  plus visuel

travailler en jour ouvré pas en calendraire

alertes = graphique

analyses des ressoruces camembert par ressources et tooltip le but est de pouvoir les deplacer

ajout de groupe indiquer le nombre de ressources (genre 30 ouvrier et les nommé automatiquement)

onglets dashboard ressources baselines et gantt

Avant test dashboard complet impression gantt avec colonne changer via colonne plus de soucis de memoire (voir crash test) + fonctionnalité indexation des planning voir ms project

revoir UI onglets pour avoir des acces plus simples 

++ graph pour rendre les differentes données visibles